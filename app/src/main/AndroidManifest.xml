<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="nl.thijswijnen.geojob">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity">
        </activity>
        <activity android:name=".UI.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".UI.SettingsActivity">
        </activity>
        <activity android:name=".UI.HelpActivity">
        </activity>
        <activity android:name=".UI.ChooseRouteActivity">
        </activity>
        <activity android:name=".UI.LanguageActivity">
        </activity>
        <activity android:name=".UI.DetailRouteActivity">
        </activity>
        <activity android:name=".UI.NavigateActivity">
        </activity>
        <activity android:name=".UI.DetailPoiActivity">
        </activity>
    </application>

</manifest>